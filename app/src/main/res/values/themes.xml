<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style
        name="Base.Theme._8227457Assignment2"
        parent="Theme.Material3.Dark.NoActionBar">

        <!-- Main Material colors -->
        <item name="colorPrimary">@color/accent_magenta</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="colorSecondary">@color/accent_purple</item>
        <item name="colorOnSecondary">@color/white</item>

        <item name="colorSurface">@color/surface_dark</item>
        <item name="colorOnSurface">@color/text_primary</item>

        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/white</item>

        <!-- App background -->
        <item name="android:windowBackground">@color/app_background</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/app_background</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Navigation bar -->
        <item name="android:navigationBarColor">@color/app_background</item>
        <item name="android:windowLightNavigationBar">false</item>

        <!-- Default font -->
        <item name="android:fontFamily">@font/exo2_regular</item>

        <!-- Keep our custom dark styling -->
        <item name="android:forceDarkAllowed">false</item>

    </style>

    <style
        name="Theme._8227457Assignment2"
        parent="Base.Theme._8227457Assignment2" />

</resources>